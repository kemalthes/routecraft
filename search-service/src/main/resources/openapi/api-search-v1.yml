openapi: 3.0.3
info:
  title: RouteCraft Search Service API
  description: Сервис семантического векторного поиска маршрутов (интеграция со Spring AI и Qdrant)
  version: 1.0.0

servers:
  - url: /api
    description: API Gateway

tags:
  - name: search
    description: Эндпоинты для полнотекстового и семантического поиска

paths:
  /search:
    get:
      tags: [search]
      summary: Поиск маршрутов по смысловому запросу
      description: Использует Spring AI для векторизации запроса и поиска ближайших соседей в Qdrant.
      operationId: searchRoutes
      parameters:
        - name: q
          in: query
          description: Текстовый запрос пользователя
          required: true
          schema:
            type: string
            minLength: 2
        - name: limit
          in: query
          description: Количество результатов
          schema:
            type: integer
            default: 3
            maximum: 10
      responses:
        '200':
          description: Список найденных маршрутов
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SearchRouteResponse'
        '400':
          $ref: '#/components/responses/BadRequestError'
        '503':
          $ref: '#/components/responses/SearchServiceUnavailableError'

components:
  responses:
    BadRequestError:
      description: Неверные параметры запроса
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SearchErrorResponse'
    SearchServiceUnavailableError:
      description: Search service is temporarily unavailable
      headers:
        Retry-After:
          description: Seconds before retrying the request
          schema:
            type: integer
            example: 10
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/GatewayErrorResponse'

  schemas:
    SearchRouteResponse:
      type: object
      description: Денормализованная карточка маршрута из Qdrant Payload
      properties:
        routeId:
          type: integer
          format: int64
          example: 125
        title:
          type: string
          example: "Тропа по горам Кавказа"
        description:
          type: string
          example: "Увлекательный маршрут для любителей активного отдыха..."
        imageUrl:
          type: string
          example: "/api/files/preview/mountains.jpg"
        score:
          type: number
          format: float
          description: Релевантность поиска (cosine similarity)
          example: 0.985

    SearchErrorResponse:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        message:
          type: string
          example: "Поисковый запрос слишком короткий"
        details:
          type: string

    GatewayErrorResponse:
      type: object
      properties:
        status:
          type: integer
          example: 503
        error:
          type: string
          example: "Service Unavailable"
        message:
          type: string
          example: "Search service is temporarily unavailable. Please try again later."
        path:
          type: string
          example: "/fallback/search-error"
